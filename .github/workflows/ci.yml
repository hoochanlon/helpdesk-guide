name: Deploy GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3  # 更新到 v3

      - name: Setup Node.js environment
        uses: actions/setup-node@v3  # 更新到 v3
        with:
          node-version: 20  # 更新到 20 或更高版本
          cache: 'yarn'  # 添加 yarn 缓存以加快构建速度

      - name: Install Dependencies
        run: yarn install --frozen-lockfile  # 添加 --frozen-lockfile 确保版本一致性

      - name: Build
        run: yarn docs:build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4  # 更新到最新版本
        with:
          branch: gh-pages
          folder: docs/.vuepress/dist
