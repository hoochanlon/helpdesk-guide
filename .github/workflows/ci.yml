name: Deploy GitHub Pages

on:
  push:
    branches:
      - master
  # 添加手动触发选项
  workflow_dispatch:
    # 可选：添加输入参数
    inputs:
      environment:
        description: '部署环境'
        required: false
        default: 'production'
        type: choice
        options:
          - production
          - staging
      skip-build:
        description: '跳过构建步骤（仅部署已构建的文件）'
        required: false
        default: false
        type: boolean

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Node.js environment
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install Dependencies
        run: yarn install

      - name: Build
        if: ${{ !inputs.skip-build || github.event_name != 'workflow_dispatch' }}
        run: yarn docs:build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.1
        with:
          branch: gh-pages
          folder: docs/.vuepress/dist
